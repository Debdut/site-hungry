{
  "name": "site-hungry",
  "version": "0.0.1",
  "main": "dist/site-hungry.js",
  "module": "dist/site-hungry.esm.js",
  "browser": "dist/site-hungry.umd.js",
  "devDependencies": {
    "@rollup/plugin-commonjs": "^11.0.1",
    "@rollup/plugin-node-resolve": "^7.0.0",
    "mocha": "^7.0.1",
    "rollup": "^1.29.0"
  },
  "scripts": {
    "build": "rollup -c",
    "dev": "rollup -c -w",
    "pretest": "npm run build",
    "test": "mocha",
    "clean": "rm -rf dist"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "cheerio": "^1.0.0-rc.3"
  }
}
